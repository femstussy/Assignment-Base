import React, { Component } from 'react';
import {
  Nav, Navbar, Form, FormControl, Image,
} from 'react-bootstrap';
import styled from 'styled-components';
import { NavigationBar } from '../components/navbar';


const Contact = () => (
  <div>
    <NavigationBar />
    <h1>Contact</h1>
  </div>
);

export default Contact;
